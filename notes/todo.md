# Todos


- Add a settings part in the backend of the app:
    - I want to autosave the content of the text editor. I want a setting to set the time interval in which autosaving happens.

- Ajouter in the Case type the Case Subject ID that you have just created
- Ajouter in the Case type the location of the thing (where it happened, so that we can look by location)

- [x] I need in the sidebar, something to handle users (clients, POI etc....)


- [x] develop all the contact workflow (domain, repository, application, handler)
- [x] develop all the client workflow (domain, repository, application, handler)
- [x] develop all the case workflow (domain, repository, application, handler)
- [ ] develop all the document workflow (domain, repository, application, handler). I need to first get the idea on how to make that work
- [x] develop all the media workflow (domain, repository, application, handler)
- [x] simplify the middleware workflow so that I have only the necessary roles for the API (admin, client/guest/visitor [the name is not clear for now])



- for client to connect to the api and get their case they will inform the case ID (or something that looks like it) + some token sent by email



I also do not see the "A 2rem gap with a subtle shadow effect to visually separate pages". I would like clearly multiple pages if that is possible. 
One page, a gap between them and then another page and repeat that for as much content as we have.


## Documents essentiels au dossier

- Devis (nom de la personne)
- Mandat (type de mission et la personne pour qui tu fais la mission)
- Facture
- Ordre de mission
- Rapport 
- Planche photos

- Devis 
- Mandat 
- Facture
- Ordre de mission
- Rapport 
- Planche photos

- Contrat de prestation de service




  We can work on the configuration management and missing operational layer

