/**
 * Mock data for the photos page.
 *
 * Retained only for reference during development.
 * The photos UI now loads data from the real API via fetchCaseMedia().
 */

import { type Image } from "./types";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const CASE_ID = "unused";

export const images: Image[] = [];
